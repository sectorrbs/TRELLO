export const mutations = {
    setList(state, list) {
        state.list = list
    },

    setCardId(state, cardId) {
        state.cardId = cardId
    },

    setCardInfo(state, cardInfo) {
        state.cardInfo = cardInfo
    }
}
